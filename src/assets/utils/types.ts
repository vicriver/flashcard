export type LanguageProps  = {
    id: number
    endpoint: string
    label: string
    native: string
}

export type CardProps = {
    id: number
    romaji: string
    character: string
}